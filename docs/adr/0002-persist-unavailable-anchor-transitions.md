# Persist unavailable Anchor transitions

An authorized Anchor Orphan Report appends explicit unavailable state with immutable Anchor Context and no placement coordinates, while a later owner-authorized Anchor Replacement restores a current location on the same Thread; this prevents restart from resurrecting stale coordinates and keeps recovery auditable.
